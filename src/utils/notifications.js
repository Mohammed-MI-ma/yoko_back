// utils/notifications.js

const notifyUserInApp = (email, resetToken) => {
  // Placeholder implementation for notifying the user within the application
  console.log(`Notifying user with email ${email} to reset their password.`);
  console.log(`Reset token: ${resetToken}`);
};

module.exports = {
  notifyUserInApp,
};
